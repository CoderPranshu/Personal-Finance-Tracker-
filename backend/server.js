/**
 * server.js (legacy)
 * ------------------
 * You originally started with this file.
 *
 * The real backend entry point is now:
 * - `src/server.js`
 *
 * We keep this file so existing references don't break,
 * but it simply forwards to the new entry point.
 */

require("./src/server");
