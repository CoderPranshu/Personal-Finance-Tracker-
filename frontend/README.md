# Frontend (React + Tailwind)
This is the React UI for the Personal Finance Tracker.

## Run
From `frontend/`:
- `npm install`
- `npm run dev`

## Backend connection (dev)
This frontend uses a Vite proxy:
- `/api/*` → `http://localhost:5000/api/*`

